import {ButtonContainer} from './styled'

const Button = ({background}: {background: string}) => {
  return <ButtonContainer background={background}>
    BUTTON
  </ButtonContainer>
}

export default Button